```swift
let prioridades = ["receita", "retencao"]
// um comentário
print(prioridades.count)
```

```mermaid
graph LR
  A[REESCRITO] --> B[Executar] --> C[Medir]
```

Inline $E = mc^2$ e display:

$$\int_0^\infty e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2}$$

> **Live reload funcionou.**
