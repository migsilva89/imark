# Build output
.build/
dist/
*.xcodeproj

# Generated by renderer/build.mjs — rebuilt from renderer/src on every build
Resources/

# Node
renderer/node_modules/

# macOS
.DS_Store
renderer/.esbuild-meta.json

# Design notes, plans, working documents. They stay on disk and out of the
# repository — the decisions in them are ours, and half of them name people.
docs/
